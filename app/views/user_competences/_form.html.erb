<%= form_for(@user_competence) do |f| %>
  <% if @user_competence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_competence.errors.count, "error") %> prohibited this user_competence from being saved:</h2>

      <ul>
      <% @user_competence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :competence_id %><br>
    <%= f.text_field :competence_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
