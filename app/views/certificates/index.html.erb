<p id="notice"><%= notice %></p>

<h1>Listing Certificates</h1>

<table>
  <thead>
    <tr>
      <th>Certificate name</th>
      <th>Certificate publisher</th>
      <th>Certificate publish date</th>
      <th>Certificate description</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @certificates.each do |certificate| %>
      <tr>
        <td><%= certificate.certificate_name %></td>
        <td><%= certificate.certificate_publisher %></td>
        <td><%= certificate.certificate_publish_date %></td>
        <td><%= certificate.certificate_description %></td>
        <td><%= certificate.user %></td>
        <td><%= link_to 'Show', certificate %></td>
        <td><%= link_to 'Edit', edit_certificate_path(certificate) %></td>
        <td><%= link_to 'Destroy', certificate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Certificate', new_certificate_path %>
